# Test profile — minimal config
orchestrator:
  openai:
    api-key: ""
    model: gpt-4o
    api-base: ""
  parser:
    url: http://localhost:9999
    api-key: test-key
    timeout-seconds: 5
  otel:
    endpoint: ""
  demo:
    pdf-url: http://localhost:8000/test.pdf
    mode: direct

spring:
  main:
    banner-mode: off
